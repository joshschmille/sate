package main

import "log"

// writeLogMarkdown accepts a string of any length, and appends the string to the log file.
func writeLogMarkdown(s string) {
	// TODO: Convert s to Markdown based on its content.
	log.Println(s)
}
